Смотри, у нас в JavaScript всё является объектами
функции являются объектамми, так как у них есть свойство .name.
Cтроки являются объектами, так как у них есть методы:
 .toLowerCase(), .slice, .substr и тп

Не знаю как у вас во Vue, но я думаю что так тоже можно сделать.

Создать компонент Button, а для него прописать методы ClearButton и SortedButton,

В общем я виду к тому что это может выглядеть следующим образом

// import { Button } from "/src/Button";

и использовать это вот так

<div>
    <Button.SortedButton />
</div>

В данном случае мы можем для функции Button назначить её свойство SortedButton
Надеюсь понятно объяснил

И в дальнейшем использовать это :D

Всё это ведёт к модульность и единой точке входа, а это уже паттерн проектирования Singleton.